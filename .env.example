# Supabase (shared with the city dashboard / firmware)
# URL is public — it's visible in any Supabase frontend.
VITE_SUPABASE_URL=https://zywpdxnwkvotsyjisjwg.supabase.co

# Anon key (PUBLIC, safe for client). NEVER put the service_role key here.
# Copy from Supabase dashboard -> Project Settings -> API -> "anon public" key.
VITE_SUPABASE_ANON_KEY=

# Optional: Mapbox token (already configured elsewhere if you've used the app)
# VITE_MAPBOX_TOKEN=
